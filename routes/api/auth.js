const express = require('express');
const router = express.Router();
//const auth = require('../../middleware/auth');

// @route   GET api/auth/
// @desc    Tests route
// @access  Public
//router.get('/', auth, (err, res) => res.send('Auth Route'));

module.exports = router;